${project.groupId}.${project.artifactId}.groupId = ${project.groupId}
${project.groupId}.${project.artifactId}.artifactId = ${project.artifactId}
${project.groupId}.${project.artifactId}.version = ${project.version}
${project.groupId}.${project.artifactId}.name = ${project.name}
${project.groupId}.${project.artifactId}.repository = ${svn.repository}
${project.groupId}.${project.artifactId}.path = ${svn.path}
${project.groupId}.${project.artifactId}.revision = ${svn.revision}
${project.groupId}.${project.artifactId}.mixedRevisions = ${svn.mixedRevisions}
${project.groupId}.${project.artifactId}.committedRevision = ${svn.committedRevision}
${project.groupId}.${project.artifactId}.committedDate = ${svn.committedDate}
${project.groupId}.${project.artifactId}.status = ${svn.status}
${project.groupId}.${project.artifactId}.specialStatus = ${svn.specialStatus}
${project.groupId}.${project.artifactId}.buildDate = ${timestamp}
